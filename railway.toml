[build]
builder = "RAILPACK"
ignoreFile = ".railwayignore"

[deploy]
startCommand = "pnpm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

[env]
NODE_ENV = "production"
NODE_OPTIONS = "--max-old-space-size=512"
